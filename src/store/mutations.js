export default {
  setUserInfo (state, value) {
    state.userInfo = value
    console.log(state.userInfo)
  },
  setRetailerInfo (state, value) {
    state.retailerInfo = value
    console.log(state.retailerInfo)
  }
}
